apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minimal-ingress

spec:
  rules:
    - host: localkube.info
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: testservice
                port:
                  number: 80